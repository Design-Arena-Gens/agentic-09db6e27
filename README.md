# Agentic Starter

Modern Next.js 14 + Tailwind CSS landing page ready for Vercel deployment.

## Commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run lint`

## Deployment

Deploy with `vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-09db6e27`.
